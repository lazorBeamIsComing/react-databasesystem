{
    "name": "management",
    "version": "1.0.0",
    "scripts": {
        "client": "cd client && yarn start",
        "server": "nodemon server.js",
        "dev": "concurrently --kill-others-fail \"yarn server\" \"yarn client\""
    },
    "dependencies": {
        "body-parser": "^1.20.0",
        "express": "^4.18.1",
        "nodemon": "^2.0.16"
    },
    "devDependencies": {
        "concurrently": "^4.0.1"
    }
}
